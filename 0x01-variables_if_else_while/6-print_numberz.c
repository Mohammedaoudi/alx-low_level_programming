#include <stdio.h>

/**
 * main - Entry point
 *
 * c program prints all single dig.... using putchar
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 48;

	for (n ; n < 58; n++)
		putchar(n);
	putchar('\n');
	return (0);
}

