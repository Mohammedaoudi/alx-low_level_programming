#include "main.h"

/**
 * _abs - fuction that computes the absolute value of an integer
 * @n: the int wich is the parametr of the function
 * Return: the abs value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
